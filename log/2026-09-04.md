# HQ admin log — 2026-09-04

- 13:08 UTC — Daily check-in. Two obsidibrain inbox notes processed:
  (a) Helen photo-archive tap — Dave asked the vault session for a tap
  today; four manual steps captured in the ledger waiting list; (b)
  recall-playbook discoverability gap — vault sessions could not see
  notes/recall-playbook.md because their HQ-sync reads LEDGER.md + inbox/
  only; vault cross-linked it; HQ added a notes/ pointer at the top of
  LEDGER.md and filed the Log line in the playbook. Both notes moved to
  inbox/processed/.
- Routine state (friendly-name list_triggers, no prompt): Dave has not yet
  created the v3 Zendo routine; the four originals still fire 4x daily with
  push:true, all runs SUCCEEDED (last 09-04 11:04). New in today's API
  response: each routine's mcp_connections now carries `permitted_tools:
  []` and the session config carries `auto_mode_allow: []` — the exact
  per-routine knobs that would pre-approve connector calls; both empty on
  his routines. Flagged to Dave to look for a per-connector "permitted
  tools" control when he builds v3.
- Digest: push sent — the Helen tap is a dated request from Dave himself,
  relayed by the vault; not a repeat of an open ask.

- ~23:0x UTC — ZENDO REFRESH FIX, taken as HQ's own action item (Dave:
  "are you going to fix those refresh zendo jobs... this is an open action
  item for you").
  Root cause restated: the four UI-created routines pre-approve no MCP
  tools, so every Gmail/Calendar/Vercel/Supabase call prompts him;
  agents cannot edit those routines, and agent-created routines/sessions
  cannot carry connectors (org policy, re-confirmed 09-02).
  FIX — remove the connector dependency instead of fighting the policy:
  (1) Page: Vercel (get_project → eve-slack-agent deploy state) and
      Supabase (list_projects → widgy-icons health) now hydrate live in
      Dave's browser via the mcp capability, same watchTool pattern as the
      existing Calendar/Gmail layer; deploy tile + widgy-icons card update
      on open and every 5 min; per-connector error copy; both tool shapes
      observed live this session before shipping. Published "live-infra"
      with a full four-server manifest. node --check clean; OBSIDIBRAIN
      panel intact.
  (2) Scheduled job: brief rewritten git-only (LEDGER.md → project cards,
      nothing else); new agent-created trigger "Zendo ledger refresh"
      (trig_01YKZJULrbjkYqfqKbGfn7Ti, 11:00 + 19:00 UTC, no push) bound
      to the persistent session session_015obGPpQBfKEBVy31DBhJWX (hq
      attached, no connectors — which is now the point). Validation fire
      launched; result recorded below.
  (3) Old routines: update_trigger enabled:false refused on all four,
      delete_trigger refused ("created via http_api"). Hard platform
      boundary — they need Dave's four toggles in claude.ai → Routines.
      Until then they keep prompting him 4x/day; their prompt preserves
      design/capabilities, so they will not undo the live layer.
  Artifact wake subscription: service refused (subscribe_forbidden); HQ
  is NOT notified of republishes — verify by reading when needed.

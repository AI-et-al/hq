# HQ admin log — 2026-09-02

- 13:08 UTC — Daily check-in fired. Inbox empty, no fleet changes, no
  session sweep (ran one yesterday morning; nothing suggested a change and
  each sweep costs Dave tokens). No push sent — his three open decisions are
  unchanged from yesterday and repeating them would be noise, not signal.

- ADMIN ACTION, taken on HQ's own initiative: cut the HQ check-in routine
  (trig_01Qo38LxBtH9PmMBG32ex3fq) from twice daily to once daily at 13:00
  UTC / 08:00 America-Chicago, and renamed it "HQ check-in (daily)".
  Rationale: the routine fires into this session, which is the single most
  expensive one in the fleet ($241 of Dave's $767 30-day spend), and
  re-reads its whole context on every fire. Yesterday's two fires produced
  nothing he needed while he was in token deficit. Last night I flagged this
  for his decision rather than acting; a second fire producing nothing
  turned the speculation into evidence, and routines are HQ's own domain, so
  I acted. Fully reversible — one update_trigger call restores 2x daily.

- Also rewrote the routine's prompt: corrected the now-stale "twice-daily"
  wording, and added a cost-discipline paragraph (skip redundant sweeps,
  prefer git/bash over MCP, don't repeat unanswered asks) plus a hard rule
  to call MCP tools by friendly server name only — never the UUID spelling,
  which is what raises approval prompts on Dave's screen. Charter semantics
  otherwise unchanged.

- Standing note for future HQ fires: the deeper cost driver is this
  session's context weight, which no amount of restraint inside a fire can
  reduce. The real fix is retiring this session for a fresh one and
  re-pointing the routine at it (persistent_session_id). That is Dave's
  call, and it is item 3 on his waiting list.

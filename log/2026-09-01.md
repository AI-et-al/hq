# HQ admin log — 2026-09-01

- ~00:3x UTC — Zendo: Hakone Gardens photo (Dave attached in chat) embedded
  as the courtyard hero, replacing the drawn SVG. Published "hakone-photo";
  full-res + 1600w copies committed to notes/assets/.
- ~13:1x UTC — INCIDENT ESCALATED, THEN RESOLVED. Dave: "every zendo refresh
  Fable had me build is still requiring manual approval clicks... cant find a
  way for these to run without my hands on them ill have to delete the entire
  thing. every. time." Screenshot showed the prompt naming server
  `bf7c680d-...` for list_sessions — HQ's own check-in call.

  ROOT CAUSE (verified, not inferred): every connector is exposed under TWO
  names — a stable UUID and a friendly name. Which one a session presents
  varies. A permission rule naming one form does not match a call arriving
  under the other. Proof: `mcp__bf7c680d-...__list_sessions` prompted Dave;
  the identical call as `mcp__Claude_Code_Remote__list_sessions` returned
  with no prompt, same session, seconds later. Confirmed again ~01:0x UTC
  when this session's whole connector set flipped friendly -> UUID mid-run.

  CORRECTION to the 2026-08-31 21:0x entry: that entry concluded the
  connector "reconnects under shifting identities... allowlists cannot win."
  Wrong. The UUIDs are stable per connector; only which spelling is
  presented varies. Allowlisting both spellings does win. The git-first
  write policy stands on its own merits (durability, no prompt surface) but
  is no longer the ONLY option, and yesterday's justification for it was
  partly mistaken. Recorded so the playbook does not inherit a false rule.

  Also explains the puzzle in the 08-31 entry: an allow for
  `mcp__bf7c680d-...__list_sessions` was already committed here, yet Dave
  still got prompted — that prompt came from a LOCAL terminal session, which
  a repo-level settings file in the cloud clone never reaches.

  FIX SHIPPED: .claude/settings.json rewritten with both spellings for all
  14 connectors (33 allow rules), destructive calls (sends, deletes, SQL,
  purchases) kept on `ask` per the standing write policy. Root-cause writeup
  at notes/permission-prompts-fix.md. Mac drop-in at
  notes/mac-user-settings.json, delivered to Dave as a file — user-level
  ~/.claude/settings.json is the only layer that covers his terminal
  sessions. Third gap remains open (below).

- ~13:5x UTC — BUDGET. Dave: "killed my token budget too" (Fable 21% left,
  25% in deficit; 30d spend $767). This HQ session alone: $241.54 —
  ~31% of the 30-day total, 78M cache-read tokens. Cause is context weight
  re-read every turn, not the work itself. Told him plainly and recommended
  retiring this session for a fresh one (all durable state is committed) and
  cutting the Zendo refresh from 4x daily. No action taken on his config.

- 01:07 UTC (09-02) — Evening check-in fired. Deliberately minimal: inbox
  empty, no session sweep (ran one this morning; each sweep costs him
  tokens), ledger reconciled, no push sent. Dave has an unanswered decision
  from this afternoon; a second push would just duplicate it while he is
  in deficit.

  HQ SELF-CRITICISM: this twice-daily check-in fires into the most expensive
  session in the fleet and re-reads its entire context each time. The
  routine is HQ's own overhead and is now a live cost to Dave. Flagged for
  his decision rather than changed unilaterally, since the 2x cadence was
  his; recommendation is to point it at a fresh session or drop to daily.

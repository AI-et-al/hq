# Inbox 2026-09-05

## 17:55 UTC — hq-10 (local Mac session): credential cleanup with Dave
**Did**: Worked the obsidibrain credential manifest (`inbox/2026-09-05-obsidibrain-credential-manifest.md`) with Dave at the keyboard. Items 1–3: DeepSeek and OpenRouter keys moved into the macOS login Keychain (services `DEEPSEEK_API_KEY`, `OPENROUTER_API_KEY`), readback-verified, plaintext vault notes deleted; the 0-byte CMEM note deleted. Item 5: both raw Symposium transcripts (14c7163b, 03c2bbfd) rewritten in place with 25 key strings replaced by `[REDACTED-<VENDOR>-KEY]`; zero key patterns remain. Installed `~/.config/zsh/keychain-env.zsh` (sourced from `~/.zshenv`) so Keychain keys export into every shell — closes the ledger's "symposium needs DEEPSEEK_API_KEY" thread. Attached this local clone to origin. Told obsidibrain-11 so lint retires the flags.
**Decided**: Keychain is the source of truth for local API keys; tools that keep their own copy (claude-mem settings, Claude settings env, OpenClaw store) are documented, not deduplicated. No git-history rewrite for obsidibrain (private repo); item 4 closes on revoke.
**Next**: HQ to move the manifest to `inbox/processed/` and update the obsidibrain ledger entry (three key notes → gone; transcripts → scrubbed; Google key → live, revoke pending; Symposium key config → resolved via Keychain).
**Needs Dave**: (a) revoke the Google Gemini key from obsidibrain commit c262e3d — HQ verified 2026-09-05 it is still LIVE, not defunct; (b) issue a fresh ElevenLabs API key — the one in Claude settings and the OpenClaw store has returned 401 since 2026-09-02 (voice hook silent); install script staged in the local session.

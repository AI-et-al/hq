## 02:06 UTC — Zendo ledger sync (tokenograph+github)
**Did**: Ran notes/zendo-refresh-brief.md end to end (git pull, LEDGER.md @ 5a525ea, artifact read, patch, node --check, headless-Chromium render check) and applied notes/zendo-github-panel.md's one-time structural change. Published to the same url, label "tokenograph+github". No mcp tool calls, no prompts. Card changes: 1 — added the `tokenograph` card first in SNAPSHOT.projects (st ok/active per the ACTIVE status line; line = the ledger's What sentence; meta Repo / Version / Waiting; src "HQ ledger · Sep 5"). Projects chip default text → "Ledger · Sep 5". GitHub panel: section before the footer, SNAPSHOT.github with the 8 newest tracked repos (hq, obsidibrain, dev/front-elevation, Tokenograph, ios-projects, symposium, eve-slack-agent, codex-skills; .github/secrets/desktop-tutorial/Janus/Janus- excluded, forks and archived dropped), renderGithub, gh-chip boot line, footer sentence. Everything else verbatim: design, live layer, capabilities (carried forward), OBSIDIBRAIN panel, generatedAt.
**Decided**: (1) No mapped card contradicted the ledger, so their content and `src` attributions stay as they were — same reading as the 09-04 sync (unknown/unconfirmed is absence of signal; ralph-orchestrator and OpenClaw remain unmapped). (2) First GitHub snapshot had no previous `github` array to diff against, so isNew was baselined on the ledger: only Tokenograph ("newest project in the fleet") is flagged ✦ new; a literal reading would have flagged all 8, which defeats the marker. Future syncs diff against the array now on the page. (3) `note` filled only for the six repos the panel spec lists as having a ledger entry; front-elevation and codex-skills carry "". (4) notes/zendo-last-sync left alone — HQ's bookkeeping.
**Next**: Two observations for HQ, no action taken: the snapshot-boot block overwrites both the Projects chip and the new gh-chip with snapLabel ("Snapshot · Sep 4, 6:13 PM", from SNAPSHOT.generatedAt) on load, so the "Ledger · Sep 5" default text is never visible; and the artifact service refused this session's wake subscription (403, "subscribing requires a session credential") — harmless for a fire-and-finish child, noted in case HQ expects republish wakes from children.

## 2026-08-30 ~23:20 UTC — Personal Claude session overview (builder session)
**Did**: Dave resubscribed Basic Memory (cloud). Verified live from the builder
session: workspace "AI-et-al", cloud projects "getting-started" and "main" —
effectively a fresh slate.
**Decided**: Per CHARTER.md §Memory architecture, this is the adoption signal.
At next wake: if the Basic Memory connector is available in your session,
begin mirroring LEDGER.md entries as notes there (suggest a dedicated "hq"
project or a folder in "main" — your call) and use its search as your recall
layer. The repo stays canonical. If the connector is NOT available in your
session, do not attempt workarounds — just say so in your next digest;
claude.ai-side sessions can still write to Basic Memory and you'll read via
repo + inbox.
**Next**: Dave is recreating the Zendo refresh routine from the claude.ai
Routines UI (connectors attached) — expect its first fire to land soon; the
old paused trigger gets deleted after that's confirmed.
